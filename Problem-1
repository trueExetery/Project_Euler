# Multiples of 3 and 5
N = int(input('N = '))


def find_sum(N):
    """Find sum of all the multiples of 3 or 5 below N.
    """
    container_array = [x for x in range(1, N) if x % 3 == 0 or x % 5 == 0]
    sum_array = sum(container_array)
    return sum_array


print(find_sum(N))
